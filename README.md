# Barefoot Pictures Digital Brochure

Responsive static brochure for Barefoot Pictures.

## Local preview

```bash
python3 -m http.server 8080
```

Open `http://localhost:8080`.

## Deploy

This project is configured for Netlify using `netlify.toml` with `publish = "."`.
